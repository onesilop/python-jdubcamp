{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "python -m pip install --disable-pip-version-check -r requirements.txt && python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
